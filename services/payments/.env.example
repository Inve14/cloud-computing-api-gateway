# ============================================================
# Payments Service — Environment Variables Template
# ============================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# NEVER commit .env to version control.
# ============================================================

# Runtime environment. Controls log format and dotenv loading.
# Values: development | test | production
NODE_ENV=development

# Port the Fastify HTTP server listens on.
PORT=3004

# Minimum log level.
# Values: trace | debug | info | warn | error | fatal | silent
LOG_LEVEL=info

# Option A (preferred): a single connection string.
DATABASE_URL=postgresql://payments_user:payments_pass@localhost:5432/payments_db

# Option B: individual parameters (used when DATABASE_URL is not set).
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=payments_db
# DB_USER=payments_user
# DB_PASSWORD=payments_pass

# Connection pool tuning
DB_POOL_MAX=10
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=3000
